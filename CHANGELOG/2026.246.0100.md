# 2026.246.0100 — fix: drop --locked from the himalaya cargo install

## Summary

`cargo install himalaya --locked` uses the himalaya crate's Cargo.lock, which
pins `chacha20 v0.10.1` — a crate yanked from crates.io. Every build composing
this candy (the openclaw-full metalayer among them) emitted the warning
`package chacha20 v0.10.1 in Cargo.lock is yanked in registry crates-io`.

Fix: drop `--locked` so cargo resolves the latest compatible transitive
versions, avoiding the yanked crate. The build remains reproducible at the
himalaya version level (cargo install pins the crate version from the command).
The candy's own CalVer moves because changed content must ship under a new
version.

## How tested

```
$ /tmp/charly-ci/bin/charly box validate
exit 0
```

Run with the charly version this repo's own `deploy.yml` clones and builds
(v2026.238.1242). The runtime path (the `cargo install himalaya` without
`--locked`) is exercised by the consuming `check-openclaw-full-pod` bed in the
distro-cachyos repo, which builds the openclaw-full image composing this candy —
that R10 evidence lands with the distro-cachyos PR.

## Rulebook compliance

- **R0 (skills)** - `/charly-image:layer` for the candy/plan model;
  `/charly-internals:git-workflow` for PR-only landing and tag-on-merge.
- **R1 (RCA)** - the warning was root-caused: the `--locked` flag pins the
  yanked chacha20 v0.10.1 from the crate's Cargo.lock; dropping it lets cargo
  resolve fresh.
- **R2 (no deferral)** - the fix ships now, eliminating the warning at the source.
- **R3 (no duplication)** - the install command is named once in the plan.
- **R4 (no workarounds)** - the lock is dropped, not the warning allowlisted.
- **R5 (hard cutover)** - the `--locked` path is gone; no old path remains.
- **R6 (git safety)** - feature branch, no force-push, no hook bypass.
- **R7 / R10 (prove the gate)** - `charly box validate` on the committed tree with
  the repo's own pinned charly; the runtime path is R10-proven by the consuming bed.
- **R8 (generation)** - N/A: no generated artifact, no CUE input in this repo.
- **R9 (artifact integrity)** - N/A: no image is built here.
- **ADE (candy description/plan)** - `description:` and `plan:` updated in
  lockstep with the change they describe.
- **SDD (CUE source)** - N/A: no schema authored or generated here.

## Change classification

- **Change class:** candy build-step fix (cargo install flags). Config only.
- **Verification gate:** `charly box validate` under the repo's pinned charly, exit 0
- **Attribution tier:** `analysed on a live system` - the gate was executed on a
  real checkout with the exit code recorded; the runtime path is proven by the
  consuming `check-openclaw-full-pod` bed (R10 evidence pasted in that PR)

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
